"use client";

import React from "react";
import AuthModal from "../modal/auth-modal";

const AuthClient = () => {
  return (
    <div className="flex items-center justify-center h-[calc(100vh-5rem)] pointer-events-none select-none"></div>
  );
};

export default AuthClient;
